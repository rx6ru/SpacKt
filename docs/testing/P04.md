# P04: per-connection tier machine

Status: GREEN. The pure policy is implemented; transport integration follows in P05.

An independent author wrote tests from the protocol without reading implementation.
The reviewed RED checkpoint was8c23601. Public stubs compiled and failed runtime assertions.
A separate review checked the policy implementation and its tests.

Review found two additional evidence-timer cases. Independent regressions failed before fixes:
- Invalid reports must break dwell evidence without refreshing report liveness.
- Missing-report fallback must clear dwell evidence even when the tier is already minimal.

## Verification

- `go -C backend test -race ./...`: passes.
- `go -C backend vet ./...`: passes.
- `go -C backend test -cover ./...`: tier statement coverage96.3%.
- `git diff --check`: passes.
- Final independent review: APPROVE.

The tests cover fractional thresholds, dead bands, report-confirmed dwell, missing-report rearming, hidden/forced precedence, backpressure, and independent machines.
The machine reads no wall clock and performs no I/O.
Its caller supplies monotonic elapsed time and evaluates timer ticks.
Transport integration must preserve that contract.
