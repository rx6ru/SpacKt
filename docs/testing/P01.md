# P01: protocol and runner evidence

Status: GREEN. Contract validation and runner baselines are implemented.

An independent author received the PRD, protocol, schemas, architecture, and public stubs.
It wrote shared raw fixtures, Go and TypeScript boundary tests, import checks, and coverage-checker tests.
A separate reviewer checked the tests before implementation.

## Verified RED

- `go -C backend test ./...`: valid wire fixtures and exact trade mapping fail with not-implemented errors. Architecture checks pass.
- `pnpm --dir web test:run`: valid wire fixtures and mapping fail with not-implemented errors. Architecture checks pass.
- `pnpm test:scripts`: the coverage checker cannot accept a valid 80% profile because its stub fails.
- `pnpm test:e2e`: one static runner-harness test passes. This is not market functionality.

Test review corrected unrelated-failure traps in byte/depth cases, round-trip assertions, and weighted Go coverage math.
The final independent RED review approved the corrected cases.

## Toolchain

Go 1.27.1 and Node 22.23.2 are installed locally.
Node 22 satisfies Next 16.3.5 and Vitest 5 requirements.
TypeScript 6.0.3 and ESLint 9.39.5 match the installed Next lint plugin peer ranges.
The latest TypeScript 7 and ESLint 10 did not match those ranges.
ESLint 9 is upstream-deprecated; this is a tooling compatibility constraint to revisit when the Next lint dependencies support ESLint 10.
Runtime packages and exact transitive versions are recorded in pnpm-lock.yaml.

## Verified GREEN

- Go unit and race tests pass. Wire statement coverage is 80.17%.
- Vitest passes 137 tests. Wire branch coverage is 86.87%; line coverage is 92.58%.
- The coverage checker passes five independent CLI tests.
- TypeScript, ESLint, Go vet, formatting, and document links pass.
- The Next.js production static export passes.
- Playwright passes the one runner smoke test.
- Final independent implementation review: APPROVE.

Review added candle alignment, explicit-null, and Unicode character-count regressions.
The independent test author reproduced each defect before its fix.
Validators now use separate shape, scalar, message, and metadata modules.

The local sandbox blocks some nested Node processes and local server binds.
The production build, CLI tests, and browser smoke passed with authorized execution outside that sandbox.
No market screen, live transport, or deployment is claimed by this checkpoint.
